# LLM-agent-b5

## 1. `_query_terms`

规范化查询词。

现场说明：指出该函数与 `B1 query` 环节的关系，并展示对应 JSON 或日志。

## 2. `_memory_score`

计算显式、关键词、重要性、时效性和访问度分项。

现场说明：指出该函数与 `读取 memory_index` 环节的关系，并展示对应 JSON 或日志。

## 3. `load_memory`

读取候选、评分排序、预算截断并记录选择原因。

现场说明：指出该函数与 `候选文档评分` 环节的关系，并展示对应 JSON 或日志。

## 4. `save_memory`

生成 Markdown 记忆、更新索引并追加日志。

现场说明：指出该函数与 `排序` 环节的关系，并展示对应 JSON 或日志。

## 5. `_safe_conversation_id`

限制会话 ID，避免形成非法路径。

现场说明：指出该函数与 `字符预算截断` 环节的关系，并展示对应 JSON 或日志。

